import '../../../../core/widgets/common_button_section.dart';
import '../../../../exports.dart';

class AccountInformationTabsCommonButtonSection extends StatelessWidget {
  const AccountInformationTabsCommonButtonSection({super.key});

  @override
  Widget build(BuildContext context) {
    return CommonButtonSection(
      buttonText: AppStrings.saveButtonText,
      onPressed: () {},
    );
  }
}
