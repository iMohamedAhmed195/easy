import '../../../../core/widgets/common_button_section.dart';
import '../../../../exports.dart';

class AccountInformationCommonButtonSection extends StatelessWidget {
  const AccountInformationCommonButtonSection({super.key});

  @override
  Widget build(BuildContext context) {
    return CommonButtonSection(
      buttonText: "Save",
      onPressed: () {},
    );
  }
}
